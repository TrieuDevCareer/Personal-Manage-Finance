version: '3'

services:
  backend:
    build: ./api
    ports:
      - "5000:5000"
    env_file:
      - ./api/.env
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - backend